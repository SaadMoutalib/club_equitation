# Club Equitation

API for a Horse Riding Club android APP.

## Hos to use : Clone the repository, install node packages and run API

```
//on local
git clone https://github.com/zowe/sample-node-api
cd sample-node-api
npm install
npm start
```
